#include "main.h"
/**
 * _abs - compute the absolute value of an intenger
 * @n: the in that is checked
 * reture: the absolute value of int
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	return (-n);
}
